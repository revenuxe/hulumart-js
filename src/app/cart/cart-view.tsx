"use client";

import Image from "next/image";
import Link from "next/link";
import { ArrowRight, Minus, Plus, ShoppingBag, Trash2 } from "lucide-react";
import { TopBar } from "@/components/TopBar";
import { useCart } from "@/lib/cart-store";

export function CartView() {
  const { items, ready, updateQuantity, removeItem, subtotal } = useCart();
  if (!ready) return null;
  return <div className="min-h-dvh bg-background pb-32"><TopBar /><main className="mx-auto w-full max-w-2xl px-5 py-8 md:px-8"><h1 className="font-display text-3xl leading-tight md:text-5xl">Your cart</h1>
    {items.length === 0 ? <div className="mt-10 flex flex-col items-center text-center"><div className="grid h-16 w-16 place-items-center rounded-full bg-muted"><ShoppingBag className="h-7 w-7 text-muted-foreground" /></div><p className="mt-4 text-sm text-muted-foreground">Your cart is empty.</p><Link href="/categories" className="mt-5 inline-flex items-center gap-1.5 rounded-full bg-gradient-brand px-6 py-3 text-sm font-bold text-primary-foreground shadow-glow">Browse products <ArrowRight className="h-4 w-4" /></Link></div> : <><div className="mt-6 space-y-3">{items.map((item) => <div key={item.id} className="flex gap-3 rounded-3xl border border-border bg-card p-3 shadow-card"><div className="relative h-24 w-24 shrink-0 overflow-hidden rounded-2xl"><Image src={item.image} alt={item.serviceName} fill className="object-cover" /></div><div className="min-w-0 flex-1"><p className="text-[11px] font-semibold uppercase tracking-wide text-muted-foreground">{item.categoryName}</p><h3 className="truncate text-sm font-bold">{item.serviceName}</h3><p className="mt-1 text-xs text-muted-foreground">Used item · ₹{item.unitPrice.toLocaleString("en-IN")}</p><div className="mt-2 flex items-center justify-between"><div className="inline-flex items-center gap-3 rounded-full bg-muted px-1.5 py-1"><button onClick={() => updateQuantity(item.id, item.quantity - 1)} className="grid h-9 w-9 place-items-center rounded-full bg-card shadow-sm" aria-label="Decrease quantity"><Minus className="h-3 w-3" /></button><span className="min-w-4 text-center text-xs font-bold">{item.quantity}</span><button onClick={() => updateQuantity(item.id, item.quantity + 1)} className="grid h-9 w-9 place-items-center rounded-full bg-card shadow-sm" aria-label="Increase quantity"><Plus className="h-3 w-3" /></button></div><span className="text-sm font-black text-gradient-brand">₹{(item.unitPrice * item.quantity).toLocaleString("en-IN")}</span></div></div><button onClick={() => removeItem(item.id)} aria-label={`Remove ${item.serviceName}`} className="grid h-10 w-10 shrink-0 place-items-center self-start rounded-full border border-border bg-card text-muted-foreground hover:text-destructive"><Trash2 className="h-4 w-4" /></button></div>)}</div><div className="fixed inset-x-0 bottom-0 z-40 border-t border-border/60 bg-background/95 pb-[max(0.75rem,env(safe-area-inset-bottom))] pt-3 backdrop-blur-lg md:static md:mt-8 md:border-0 md:bg-transparent md:p-0"><div className="mx-auto max-w-2xl px-5 md:px-0"><div className="rounded-2xl border border-border bg-card p-4 shadow-card"><div className="flex justify-between text-sm text-muted-foreground"><span>Items subtotal</span><span>₹{subtotal.toLocaleString("en-IN")}</span></div><div className="mt-3 flex items-center justify-between border-t border-border pt-3"><span className="text-sm font-bold">Total</span><span className="text-xl font-black text-primary">₹{subtotal.toLocaleString("en-IN")}</span></div></div><button disabled className="mt-3 flex w-full items-center justify-center gap-2 rounded-full bg-muted px-6 py-3.5 text-sm font-bold text-muted-foreground">Checkout coming soon <ArrowRight className="h-4 w-4" /></button></div></div></>}</main></div>;
}
