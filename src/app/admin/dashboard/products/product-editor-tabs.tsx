"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import type { Database, Json } from "@/lib/supabase/types";
import { createClient } from "@/lib/supabase/client";
import { GalleryUploadField } from "@/components/admin/GalleryUploadField";

type Product = Database["public"]["Tables"]["products"]["Row"];
type Category = { id: string; name: string };
type Subcategory = { id: string; name: string; category_id: string };
type LibraryItem = Database["public"]["Tables"]["product_content_library"]["Row"];
type Faq = { question: string; answer: string };
type Props = { product: Product | null; categories: Category[]; subcategories: Subcategory[]; library: LibraryItem[]; defaultCategoryId?: string; defaultSubcategoryId?: string };
type Tab = "product" | "content" | "media" | "seo";
const field = "mt-1 w-full rounded-xl border border-border bg-background px-3 py-2.5 text-sm outline-none focus:ring-2 focus:ring-primary";
const textarea = `${field} min-h-24 resize-y`;
const list = (value: string) => value.split("\n").map((item) => item.trim()).filter(Boolean);
const slugify = (value: string) => value.toLowerCase().trim().replace(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
const parseFaqs = (value: Json): Faq[] => Array.isArray(value) ? value.filter((item): item is Faq => typeof item === "object" && item !== null && typeof (item as Faq).question === "string" && typeof (item as Faq).answer === "string") : [];
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block text-sm font-semibold"><span>{label}</span>{children}</label>; }
function Panel({ active, children }: { active: boolean; children: React.ReactNode }) { return <section className={active ? "space-y-4" : "hidden"}>{children}</section>; }
function ContentSelector({ kind, label, items, onApply }: { kind: string; label: string; items: LibraryItem[]; onApply: (id: string, kind: string) => void }) { return <Field label={`Apply saved ${label}`}><select defaultValue="" onChange={(event) => onApply(event.target.value, kind)} className={field}><option value="">Choose a saved {label.toLowerCase()}…</option>{items.filter((item) => item.kind === kind && item.is_active).map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field>; }

export function ProductEditorTabs({ product, categories, subcategories, library, defaultCategoryId, defaultSubcategoryId }: Props) {
  const router = useRouter();
  const [tab, setTab] = useState<Tab>("product"); const [saving, setSaving] = useState(false); const [uploading, setUploading] = useState(false); const [message, setMessage] = useState("");
  const [name, setName] = useState(product?.name ?? ""); const [slug, setSlug] = useState(product?.slug ?? ""); const [slugEdited, setSlugEdited] = useState(Boolean(product?.slug));
  const [categoryId, setCategoryId] = useState(product?.category_id ?? defaultCategoryId ?? ""); const [images, setImages] = useState<string[]>(product?.images ?? []);
  const [included, setIncluded] = useState((product?.included ?? []).join("\n")); const [notIncluded, setNotIncluded] = useState((product?.not_included ?? []).join("\n"));
  const [faqs, setFaqs] = useState<Faq[]>(parseFaqs(product?.faqs ?? [])); const [deliveryInfo, setDeliveryInfo] = useState(product?.delivery_info ?? ""); const [careInfo, setCareInfo] = useState(product?.care_info ?? "");
  function applyLibrary(id: string, kind: string) { const item = library.find((entry) => entry.id === id); if (!item) return; if (kind === "included_set") { setIncluded(item.included.join("\n")); setNotIncluded(item.not_included.join("\n")); } if (kind === "faq_set") setFaqs(parseFaqs(item.faqs)); if (kind === "delivery_note") setDeliveryInfo(item.body ?? ""); if (kind === "care_note") setCareInfo(item.body ?? ""); }
  function updateFaq(index: number, key: keyof Faq, value: string) { setFaqs((items) => items.map((item, i) => i === index ? { ...item, [key]: value } : item)); }
  async function submit(form: FormData) {
    if (uploading) return setMessage("Wait for image uploads to finish before saving."); if (!images.length) return setMessage("Add at least one product image before saving."); setSaving(true); setMessage("");
    const productName = name.trim(); const productSlug = slug.trim() || slugify(productName);
    const payload = { name: productName, slug: productSlug, category_id: categoryId, subcategory_id: String(form.get("subcategory_id") ?? "") || null, brand: String(form.get("brand") ?? "").trim() || null, model: String(form.get("model") ?? "").trim() || null, sku: String(form.get("sku") ?? "").trim() || null, condition_grade: String(form.get("condition_grade") ?? "good") as Product["condition_grade"], stock_quantity: Math.max(0, Number(form.get("stock_quantity") || 0)), price: Math.max(0, Number(form.get("price") || 0)), sale_price: Number(form.get("sale_price") || 0) || null, approximate_age_months: Number(form.get("approximate_age_months") || 0) || null, tagline: String(form.get("tagline") ?? "").trim() || null, description: String(form.get("description") ?? "").trim() || null, condition_summary: String(form.get("condition_summary") ?? "").trim() || null, usage_summary: String(form.get("usage_summary") ?? "").trim() || null, included: list(included), not_included: list(notIncluded), faqs: faqs.filter((faq) => faq.question.trim() && faq.answer.trim()), delivery_info: deliveryInfo.trim() || null, care_info: careInfo.trim() || null, warranty_status: String(form.get("warranty_status") ?? "none") as Product["warranty_status"], warranty_provider: String(form.get("warranty_provider") ?? "").trim() || null, warranty_coverage: String(form.get("warranty_coverage") ?? "").trim() || null, images, tags: String(form.get("tags") ?? "").split(",").map((item) => item.trim()).filter(Boolean), meta_title: String(form.get("meta_title") ?? "").trim() || null, meta_description: String(form.get("meta_description") ?? "").trim() || null, og_image_url: String(form.get("og_image_url") ?? "").trim() || null, is_active: form.get("is_active") === "on", is_featured: form.get("is_featured") === "on", is_trending: form.get("is_trending") === "on" };
    if (!productName || !categoryId || !payload.price) { setSaving(false); setTab("product"); return setMessage("Name, category, and a price are required."); }
    const supabase = createClient(); const result = product ? await supabase.from("products").update(payload).eq("id", product.id) : await supabase.from("products").insert({ ...payload, sort_order: 0 }); setSaving(false); if (result.error) return setMessage(result.error.message); router.push("/admin/dashboard/products"); router.refresh();
  }
  return <form action={submit} className="mx-auto max-w-4xl space-y-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="font-display text-2xl">{product ? "Edit product" : "New product"}</h2><p className="mt-1 text-sm text-muted-foreground">Organise the listing in focused tabs. Content library selections copy into this product.</p></div><div className="flex max-w-full overflow-x-auto rounded-full bg-muted p-1">{(["product", "content", "media", "seo"] as Tab[]).map((item) => <button key={item} type="button" onClick={() => setTab(item)} className={`rounded-full px-4 py-2 text-xs font-bold capitalize ${tab === item ? "bg-primary text-primary-foreground shadow-sm" : "text-muted-foreground"}`}>{item}</button>)}</div></div>
    <Panel active={tab === "product"}><div className="grid gap-4 rounded-3xl border border-border bg-card p-5 md:grid-cols-2"><Field label="Product name *"><input required name="name" value={name} onChange={(event) => { const value = event.target.value; setName(value); if (!slugEdited) setSlug(slugify(value)); }} className={field} /></Field><Field label="URL slug"><input name="slug" value={slug} onChange={(event) => { setSlug(event.target.value); setSlugEdited(true); }} placeholder="Auto-filled from product name" className={field} /></Field><Field label="Category *"><select required value={categoryId} onChange={(event) => setCategoryId(event.target.value)} className={field}><option value="">Select category</option>{categories.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><Field label="Subcategory"><select name="subcategory_id" defaultValue={product?.subcategory_id ?? defaultSubcategoryId ?? ""} className={field}><option value="">None</option>{subcategories.filter((item) => item.category_id === categoryId).map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></Field><Field label="Brand"><input name="brand" defaultValue={product?.brand ?? ""} className={field} /></Field><Field label="Model"><input name="model" defaultValue={product?.model ?? ""} className={field} /></Field><Field label="SKU"><input name="sku" defaultValue={product?.sku ?? ""} className={field} /></Field><Field label="Condition"><select name="condition_grade" defaultValue={product?.condition_grade ?? "good"} className={field}>{["like_new", "excellent", "good", "fair"].map((item) => <option key={item} value={item}>{item.replace("_", " ")}</option>)}</select></Field><Field label="Stock quantity"><input type="number" min="0" name="stock_quantity" defaultValue={product?.stock_quantity ?? 1} className={field} /></Field><Field label="List price (₹) *"><input required type="number" min="1" name="price" defaultValue={product?.price ?? ""} className={field} /></Field><Field label="Sale price (₹)"><input type="number" min="0" name="sale_price" defaultValue={product?.sale_price ?? ""} className={field} /></Field><Field label="Approx. age (months)"><input type="number" min="0" name="approximate_age_months" defaultValue={product?.approximate_age_months ?? ""} className={field} /></Field></div></Panel>
    <Panel active={tab === "content"}><div className="space-y-4 rounded-3xl border border-border bg-card p-5"><Field label="Short description"><input name="tagline" defaultValue={product?.tagline ?? ""} className={field} /></Field><Field label="Full description"><textarea name="description" rows={5} defaultValue={product?.description ?? ""} className={textarea} /></Field><Field label="Condition notes"><textarea name="condition_summary" rows={3} defaultValue={product?.condition_summary ?? ""} className={textarea} /></Field><Field label="Usage summary"><input name="usage_summary" defaultValue={product?.usage_summary ?? ""} className={field} /></Field><div className="grid gap-4 md:grid-cols-2"><div><ContentSelector kind="included_set" label="what’s included set" items={library} onApply={applyLibrary} /><Field label="What’s included (one per line)"><textarea value={included} onChange={(event) => setIncluded(event.target.value)} rows={5} className={textarea} /></Field></div><Field label="Not included (one per line)"><textarea value={notIncluded} onChange={(event) => setNotIncluded(event.target.value)} rows={5} className={textarea} /></Field></div><div><ContentSelector kind="faq_set" label="FAQ set" items={library} onApply={applyLibrary} /><div className="mt-2 space-y-3">{faqs.map((faq, index) => <div key={index} className="grid gap-2 rounded-xl border border-border p-3"><input value={faq.question} onChange={(event) => updateFaq(index, "question", event.target.value)} placeholder="Question" className={field} /><textarea value={faq.answer} onChange={(event) => updateFaq(index, "answer", event.target.value)} placeholder="Answer" className={textarea} /><button type="button" onClick={() => setFaqs((items) => items.filter((_, itemIndex) => itemIndex !== index))} className="text-left text-xs font-bold text-destructive">Remove FAQ</button></div>)}</div><button type="button" onClick={() => setFaqs((items) => [...items, { question: "", answer: "" }])} className="mt-3 rounded-full border border-border px-4 py-2 text-xs font-bold">Add FAQ</button></div></div><div className="grid gap-4 rounded-3xl border border-border bg-card p-5 md:grid-cols-2"><div><ContentSelector kind="delivery_note" label="delivery note" items={library} onApply={applyLibrary} /><Field label="Delivery details"><textarea value={deliveryInfo} onChange={(event) => setDeliveryInfo(event.target.value)} rows={4} className={textarea} /></Field></div><div><ContentSelector kind="care_note" label="care note" items={library} onApply={applyLibrary} /><Field label="Care details"><textarea value={careInfo} onChange={(event) => setCareInfo(event.target.value)} rows={4} className={textarea} /></Field></div><Field label="Warranty"><select name="warranty_status" defaultValue={product?.warranty_status ?? "none"} className={field}>{["none", "seller", "manufacturer", "extended"].map((item) => <option key={item} value={item}>{item}</option>)}</select></Field><Field label="Warranty provider"><input name="warranty_provider" defaultValue={product?.warranty_provider ?? ""} className={field} /></Field><Field label="Warranty coverage"><input name="warranty_coverage" defaultValue={product?.warranty_coverage ?? ""} className={field} /></Field></div></Panel>
    <Panel active={tab === "media"}><div className="rounded-3xl border border-border bg-card p-5"><GalleryUploadField value={images} onChange={setImages} pathPrefix={`products/${product?.id ?? "new"}`} onUploadingChange={setUploading} /></div></Panel>
    <Panel active={tab === "seo"}><div className="space-y-4 rounded-3xl border border-border bg-card p-5"><h3 className="font-display text-xl">Search & social sharing</h3><p className="text-sm text-muted-foreground">Leave these empty to use the product name, description, and first image.</p><Field label="Meta title"><input name="meta_title" maxLength={70} defaultValue={product?.meta_title ?? ""} className={field} /></Field><Field label="Meta description"><textarea name="meta_description" maxLength={160} rows={3} defaultValue={product?.meta_description ?? ""} className={textarea} /></Field><Field label="Open Graph image URL"><input type="url" name="og_image_url" defaultValue={product?.og_image_url ?? ""} className={field} /></Field><Field label="Search tags"><input name="tags" defaultValue={(product?.tags ?? []).join(", ")} className={field} /></Field></div></Panel>
    <section className="flex flex-wrap gap-5 rounded-3xl border border-border bg-card p-5 text-sm font-semibold"><label className="flex items-center gap-2"><input type="checkbox" name="is_active" defaultChecked={product?.is_active ?? true} /> Visible to customers</label><label className="flex items-center gap-2"><input type="checkbox" name="is_featured" defaultChecked={product?.is_featured ?? false} /> Featured</label><label className="flex items-center gap-2"><input type="checkbox" name="is_trending" defaultChecked={product?.is_trending ?? false} /> Trending</label></section>{message && <p className="rounded-xl bg-destructive/10 px-4 py-3 text-sm font-semibold text-destructive">{message}</p>}<button disabled={saving || uploading} className="rounded-full bg-gradient-brand px-6 py-3 text-sm font-bold text-primary-foreground shadow-glow disabled:opacity-50">{saving ? "Saving…" : uploading ? "Uploading images…" : "Save product"}</button>
  </form>;
}
