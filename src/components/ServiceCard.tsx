import Image from "next/image";
import Link from "next/link";
import { Star } from "lucide-react";
import type { DecorService } from "@/data/types";

export function ServiceCard({ service, size = "sm", compact = false }: { service: DecorService; badge?: "trending" | "featured"; size?: "sm" | "md"; compact?: boolean }) {
  return <Link href={`/categories/${service.categorySlug}/${service.slug}`} className="group block overflow-hidden rounded-2xl border border-border bg-card shadow-card transition-all active:scale-[0.98] md:rounded-3xl md:hover:-translate-y-1 md:hover:shadow-elevated"><div className={`relative overflow-hidden ${size === "md" && !compact ? "aspect-[4/3]" : "aspect-square"}`}><Image src={service.images[0]} alt={service.name} fill loading="lazy" sizes="(min-width: 768px) 25vw, 50vw" className="object-cover transition-transform duration-500 group-hover:scale-105" /><span className="absolute left-2 top-2 rounded-full bg-white/90 px-2 py-0.5 text-[9px] font-bold uppercase tracking-wide text-primary shadow-sm backdrop-blur md:left-2.5 md:top-2.5 md:px-2.5 md:py-1 md:text-[10px]">Setup</span></div><div className="p-3"><h3 className={`line-clamp-2 font-bold leading-tight text-primary ${compact ? "min-h-10 text-[15px]" : "min-h-9 text-[15px]"}`}>{service.name}</h3><div className={`${compact ? "mt-1.5" : "mt-2"} flex items-center gap-1.5 text-xs`}><span className="inline-flex items-center gap-0.5 rounded-full bg-[#12ad4b] px-1.5 py-0.5 font-bold text-white"><Star className="h-3 w-3 fill-current" />{service.rating.toFixed(1)}</span><span className="truncate text-muted-foreground">{service.reviewCount.toLocaleString("en-IN")} {service.reviewCount === 1 ? "review" : "reviews"}</span></div><div className={`${compact ? "mt-2" : "mt-3 border-t border-border pt-3"} flex flex-wrap items-baseline gap-x-1.5 gap-y-0.5`}><span className="text-base font-bold text-primary">₹{service.priceDiscounted.toLocaleString("en-IN")}</span>{service.priceOriginal > service.priceDiscounted && <span className="text-[11px] text-muted-foreground line-through">₹{service.priceOriginal.toLocaleString("en-IN")}</span>}{compact && service.discountPct > 0 && <span className="text-[11px] font-bold text-[#0a9f45]">{service.discountPct}% off</span>}</div>{!compact && <span className="mt-3 flex w-full items-center justify-center rounded-xl bg-primary px-3 py-2.5 text-[11px] font-bold uppercase tracking-wide text-primary-foreground transition-colors group-hover:bg-brand-purple">Book now</span>}</div></Link>;
}
